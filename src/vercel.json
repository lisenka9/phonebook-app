{
  "version": 2,
  "builds": [
    {
      "src": "**",
      "use": "@vercel/static",
      "config": {
        "distDir": "dist/phonebook-app/browser"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}