[build]
  command = "npm install && npm run build"
  publish = "dist/phonebook-app/browser"

[plugins]
  disabled = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"